import { ReactElement } from 'react';
import './button.css';
import React from 'react';

const Input = (): ReactElement => {
  return <input className="app-input"> </input>;
};

export default Input;
