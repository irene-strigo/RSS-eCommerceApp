import 'normalize.css';

document.addEventListener('DOMContentLoaded', async function onDOMContentLoaded(): Promise<void> {
  // const appContainer: HTMLElement | null = document.getElementById('app');
  console.log('Hello world!');
});
